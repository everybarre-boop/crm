# ⚠️ 회원 개인정보 데이터 — 절대 커밋 금지
*.xlsx
*.xls
*.csv

# ⚠️ PII 가 박힌 1회성 데이터 반영 SQL — 실명·연락처가 들어있어 커밋 금지.
# (스키마 마이그레이션 SQL 은 PII 가 없으므로 정상 커밋한다.)
sql/2026-07-16_update_gwanggyo_counts.sql

# Playwright MCP 부산물 — 조작한 페이지 스냅샷이라 회원 PII 가 그대로 담긴다.
.playwright-mcp/

# Node / Next.js
/node_modules
/.next
/out
next-env.d.ts
*.tsbuildinfo

# 환경 변수 (anon 키는 공개지만, 비밀 담긴 로컬 파일은 커밋하지 않는다)
.env
.env.local
.env*.local
# automation/.env 등 하위 폴더의 .env 도 모두 차단 (관리자·스튜디오메이트 비밀번호)
**/.env
**/.env.local

# 자동화(automation/) 부산물 — 스크랩 결과·미매칭 로그엔 회원 PII 가 담긴다. 커밋 금지.
automation/*.local.json
automation/out/
automation/.auth/
playwright-report/
test-results/

# OS / 편집기 부산물
.DS_Store
Thumbs.db
desktop.ini
*.tmp
